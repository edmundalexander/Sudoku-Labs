name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Configure Git 🔧
        run: |
          git config pull.rebase false

      - name: Assemble Build Directory 📦
        run: |
          mkdir -p build/public/assets build/config build/src
          cp public/index.html public/favicon.svg build/
          cp -r public/assets/. build/public/assets/
          cp -r src/. build/src/
          cp config/config.example.js config/config.production.example.js build/config/

      - name: Inject GAS_URL Secret 🤫
        run: |
          cat > build/config/config.local.js << 'EOF'
          const CONFIG = {
            GAS_URL: '${{ secrets.GAS_URL }}',
            BASE_PATH: '/Sudoku-Labs',
          };
          window.CONFIG = CONFIG;
          EOF
        env:
          GAS_URL: ${{ secrets.GAS_URL }}

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: build
